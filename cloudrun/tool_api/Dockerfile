FROM python:3.11-slim

WORKDIR /app
COPY cloudrun/tool_api/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY app/ ./app/
COPY cloudrun/tool_api/main.py .
ENV PORT=8080
CMD ["sh", "-c", "exec uvicorn main:app --host 0.0.0.0 --port ${PORT:-8080}"]
